const userRouter = require('./userRouter');
const loginRouter = require('./loginRouter');
const summaryRouter = require('./studySummaryRouter');

module.exports = {
  userRouter,
  loginRouter,
  summaryRouter,
};